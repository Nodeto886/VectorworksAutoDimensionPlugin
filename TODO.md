# Auto Dimension Plugin Task List

## 标注功能

- [x] 快速连续标注（交互式）：首点锚定，逐点续接生成水平/垂直投影（转角）尺寸；ESC 结束。保留选中路径/线段的批量连续对齐尺寸。
- [x] 任意角度链（2026-08-13）：前两点定义链方向 θ，后续点击沿 θ 轴投影续接尺寸；ESC 结束。offset 侧边符号待冒烟实测确认。
- [x] 线对象自动标注：水平、垂直、真长和角度尺寸。
- [x] 符号定位标注：两点交互，识别符号插入点，从插入点向符号边界引出定位尺寸；非符号对象回退为整体宽高尺寸。
- [x] 交线标注：点选两点，自动识别两点间虚拟直线与图元相交的点并生成线性尺寸。
- [x] 选择对象自动标注：按对象生成总体尺寸。
- [x] 多对象中心点定位标注：按最近邻关系生成中心间距。
- [x] 多个块对象边界定位标注：对多选对象整体边界生成宽、高尺寸。
- [x] 点选封闭空间自动标注：点选封闭二维对象后生成边界尺寸。
- [x] 角度、半径、直径、弧长：线段角度；圆弧半径、直径和弧长。
- [x] 椭圆/复杂曲线标注：圆形椭圆生成半径和直径；椭圆生成长短轴尺寸；含圆弧顶点的多段线生成半径和弧长尺寸。

## 编辑功能

- [x] 转换标注：将对齐标注转换为转角（水平/垂直投影）尺寸。
- [x] 尺寸线剪齐：按源几何真实边界计算并设置 witness 自定义偏移。
- [x] 标注对齐：统一选中尺寸的起始偏移。
- [x] 标注分割和延伸：优先分割为两条尺寸，创建失败时回退到延伸。
- [x] 修正标注文字方向。
- [x] 修正标注点：规范尺寸起止点顺序。
- [x] 合并标注：创建链式尺寸。
- [x] 标注文字自动避让：基于尺寸包围框碰撞检测的多轮布局；选中源图形时同时避让源图形，密集场景沿标注线方向外推文字（纯尺寸场景行为不变）。
- [x] 重置标注文字。
- [x] 重置标注文字位置。

## 工程与审查

- [x] Vectorworks 2025 Release 构建。
- [x] Vectorworks 2026 Release 构建。
- [x] `git diff --check`。
- [ ] 在 Vectorworks 2025/2026 实例中完成所有交互回归测试。
- [ ] 在 Vectorworks 2025/2026 完成交互回归测试后，再发布正式版本。
- [x] 参数设置：模式栏「设置」按钮，可配置尺寸偏移、文字避让步长/迭代上限、交线去重阈值（Saved Settings 用户级持久化，默认=当前硬编码值）。

## 算法核心 V2（2026-08-16）

- [x] 尺度感知容差、无量纲平行判断与空间哈希交点去重。
- [x] 四角度圆统计主方向、置信度守卫、凸包最小面积旋转包围盒。
- [x] 连续路径拓扑成链、统一链偏移和区间图尺寸车道。
- [x] 顶层圆、椭圆、圆弧解析交线；半圆 `radius == halfChord` 边界修复。
- [x] 开放路径代表边使用加权集合覆盖，不再只取最长三边。
- [x] 中心标注改为局部轴正交偏好的加权生成树。
- [x] 文字避让增加离散全局布局初始解，保留 SDK 实测边界精修。
- [x] 新增纯 C++ 算法与性质回归；Ninja/GCC、MSVC 2025/2026 Release 均通过。
- [ ] 在 Vectorworks 2025/2026 实测解析圆弧 sweep 方向、旋转椭圆实体矩阵和最终文字布局边界。

## 代码评审 P2 修复（2026-08-11，来源 docs/code-review-2026-08-08.md）

- [x] P1-1：交线标注采集墙/楼板（`kWallNode/kSlabNode` 加入 `CollectPlanBounds`）。
- [x] P1-2：符号纵向插入尺寸 offset 基准错误（`maxX`→`minX`）。
- [x] P2-1：交线去重阈值相对化 `max(用户设置, 虚拟线长×1e-6)`，微缩/超大图纸不再误合并或漏合并。
- [x] P2-2：立面守卫文案改为「仅支持平面/非立面视图，当前为标准立面视图，请切换到平面视图后重试。」（与实际只拦标准立面逻辑一致）。
- [x] P2-4：剪齐模式仅选尺寸未选源图形时给出明确提示，不再静默无效/误导。
- [x] 文档：autodim-feature-design.md §1.3.1 注明圆/椭圆/弧交线标注用包围盒求交的精度限制及 W2 缓解方向。
- [ ] P2-3（需实测）：链尺寸被 kEditConvert 折叠为单条正交尺寸的行为确认，必要时守卫排除。
- [ ] P2-6（需实测）：`CreateChainDimension` 是否消费输入尺寸，决定链式合并是否产生叠加。
- [x] §3.2（W2 增强）：转换标注 offset 按 `offset/cosθ` 折算，保持尺寸线几何位置不跳位（2026-08-13 已实现，位置对照留待冒烟实测）。
- [ ] 评审冒烟清单（实测前必跑）：offset 侧边约定、斜点对渲染、链合并、链 convert、offset 折算位置对照。

## 代码评审 P2 修复（2026-08-13，来源：未提交工作评审 + 自研 SDK 文档核对）

- [x] 链合并 undo 损坏：中间链同时被 `AddAfterSwapObject` + `AddBeforeSwapObject` 注册 + `DeleteObject(true)`，Undo 会复活从未存在的中间链。已重构为「中间链绝不注册 undo、仅最终链 `AddAfterSwapObject`、回滚一律 `DeleteObject(false)`」。
- [x] 链合并回滚：失败路径 `DeleteObject(chain, true)` 会在取消的合并上残留 undo 记录。已改为无 undo 清理（所有链从未被保持注册）。
- [x] 文字避让单尺寸无效：`dimensionBounds.size() < 2` 提前返回，导致「单个尺寸 + 选中源图形」的源障碍避让永不执行。已改为 `empty()` 才返回。
- [x] 快速链立面卡死：首点锚定后进入标准立面，后续每点都重复弹窗。已在立面守卫触发时复位链状态。
- [ ] （保留，P2-6 待实测）`CreateChainDimension` 是否消费输入尺寸：合并成功路径仍按「不消费」自删中间链，若实测消费则需改。
- [ ] （保留，需实测）设置/避让相关 undo 事件在无净变化时自动关闭，`AddBothSwapObject` 冗余记录的行为确认（SDK 文档为可选项，风险低）。

## 代码审查优化（2026-08-13）

- [x] 独立几何层拒绝反向、非有限边界，并按目标轴过滤零长度尺寸。
- [x] 新增独立 C++/CTest 回归与 CI；覆盖名称映射、三轴定义、点/线退化边界、反向及无限边界。
- [x] 交线平行判断改为无量纲角度阈值，微小但明确相交的图形不再被误判为平行。
- [x] Python 数值回归同步生产代码的相对去重规则，新增微尺度与超长虚拟线案例。
- [x] 合并失败改用 `SupportUndoAndRemove` / `UndoAndRemove` 原子回滚，避免部分原尺寸已删除时留下半成品 Undo 事件。
